{
  "name": "cars-dashboard-functions",
  "private": true,
  "type": "module",
  "description": "Netlify Functions for the used-Tesla dashboard intent inbox (#14). The function source is shared with the SF-rentals site and lives at ../functions/*.mjs; the cars GitPagesPublisher copies it into the dashboard repo on publish (#49) — no hand-copy at deploy time.",
  "dependencies": {
    "@netlify/blobs": ">=7"
  }
}
